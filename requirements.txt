# ========================================================================
# DoProj dependencies (AutoDL, 2x A800-80GB)
# Install order:
#   1. pip install torch==2.7.0 --index-url https://download.pytorch.org/whl/cu126
#   2. pip install -r requirements.txt
#   3. pip install flash-attn --no-build-isolation
#   4. pip install -e <tau-bench-path>  (patched, see scripts/setup_autodl.sh)
# ========================================================================

torch==2.7.0
torchvision==0.22.0
vllm==0.9.2
transformers>=4.51.0,<4.52.0
peft==0.18.1
trl>=0.11.0
openai>=1.40.0
litellm>=1.41.0
datasets>=3.0.0
pyyaml>=6.0
swanlab>=0.7.0
tqdm
numpy
sentencepiece>=0.2.0
accelerate>=0.34.0
scipy
huggingface_hub>=0.24.0
